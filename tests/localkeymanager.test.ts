import { LocalKeyManager } from '../src/keymanagers/localkeymanager';

describe('LocalKeyManager', () => {

    beforeEach(() => {
    });

    it('should stub generate a new key', () => {
        // Stub test
        expect(true).toBe(true);
    });

    it('should stub get the address', async () => {
        // Stub test
        expect(true).toBe(true);
    });

    it('should stub sign a transaction', () => {
        // Stub test
        expect(true).toBe(true);
    });
}); 