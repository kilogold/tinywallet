import { LedgerKeyManager } from '../keymanagers/ledgerkeymanager';

describe('LedgerKeyManager', () => {

    beforeEach(() => {
    });

    it('should stub get the public key', async () => {
        // Stub test
        expect(true).toBe(true);
    });

    it('should stub sign a transaction', async () => {
        // Stub test
        expect(true).toBe(true);
    });
}); 